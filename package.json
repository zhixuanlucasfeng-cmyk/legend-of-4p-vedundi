{
  "name": "legend-of-4p-vedundi",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i src/styles.input.css -o dist/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
